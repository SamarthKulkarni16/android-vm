# android-vm
On-demand KVM-accelerated Android emulator, live-controllable via ADB/scrcpy.
Trigger the 'android-live' workflow manually from the Actions tab, watch the run log for the 'CONNECT WITH' line, then on your PC:
    adb connect <host>:<port>
    scrcpy

